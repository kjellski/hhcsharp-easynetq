using System;

namespace EasyNetQTalk.Core
{
    public static class RabbitMQConfiguration
    {
        public static String ConnectionString = "host=localhost;username=guest;password=guest;port=5672;virtualHost=/";
    }
}